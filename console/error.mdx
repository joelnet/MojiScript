---
name: error
menu: console
---

# api/console/error

`error :: a -> a`

`error` sends value to `console.error` and then returns the value.

```javascript
import { error, pipe } from 'mojiscript'

const main = pipe ([
  error,
  value => console.log (value) //=> "uh oh"
])

main ('uh oh')
```
