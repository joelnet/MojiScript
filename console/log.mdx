---
name: log
menu: console
---

# console/log

`log :: a -> a`

`log` sends value to `console.log` and then returns the value.

```javascript
import { log, pipe } from 'mojiscript'

const main = pipe ([
  log,
  value => console.log (value) //=> "Hello World!"
])

main ('Hello World!')
```
