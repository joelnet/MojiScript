---
name: W
menu: combinators
---

# combinators/W

`W :: Function -> Any -> Any`

`W` x y duplicates the argument y.

```javascript
import { W, pipe } from 'mojiscript'

const func = W (v1 => pipe ([
  v2 => console.log (v1 === v2) //=> true
]))

func (888)
```
